{
  "name": "snackbyte-speckit-engine",
  "version": "1.0.0",
  "private": true,
  "description": "snackbyte-speckit-engine — a Spec Kit lifecycle engine with swappable tracker plugs (ClickUp is the first plug). This package.json exists only to provide the local check gate (check:all / test:release); the engine and its plugs have no Node runtime dependency.",
  "scripts": {
    "lint:sh": "find .specify -name '*.sh' -not -path '*/node_modules/*' -print0 | xargs -0 -n1 bash -n",
    "test:sh": "bash .specify/scripts/bash/run-tests.sh",
    "test:release": "bash .specify/scripts/bash/run-tests.sh",
    "check:all": "npm run lint:sh && npm run test:sh"
  },
  "license": "MIT"
}
